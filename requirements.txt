# Dependencies live in pyproject.toml; this file only points pip at it, so the
# two can never drift. Installs the project in editable mode together with its
# runtime dependencies:
#
#     pip install -r requirements.txt
#
# Add the dev extra to get pytest for the unit tests:
#
#     pip install -e ".[dev]"
-e .
